// Pin the formatting to the WordPress style, so a standalone clone of this
// repository formats the same way it does inside the kehitys workspace.
module.exports = require('@wordpress/prettier-config');
